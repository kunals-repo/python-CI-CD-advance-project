name: Call Python CI/CD Workflow

on:
  push:
  workflow_dispatch:

jobs:
  call-reusable-workflow:
    uses: kunals-repo/python-CI-CD-pipeline-with-reusable-workflow/.github/workflows/python_reusable.yml@main

    with:
      python_versions: "3.9,3.10,3.11"
